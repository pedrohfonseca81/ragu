{
  "description": "Ragu knowledge base guardian: blocks the session from ending with code changes that are not reflected in the knowledge base.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/enforce.mjs\"",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
